# ===== 评估环境配置（覆盖 .env 中的对应项） =====
# judge.py 会先加载 .env，再用此文件 override=True 覆盖。
# 此处只写与生产/开发环境不同的值，LLM 等共用配置沿用 .env 不动。

# 会话签名密钥 —— 评估专用，与生产隔离
SECRET_KEY=eval-harness-dev-key-not-for-production

# 数据库 —— 评估使用独立 SQLite，不污染开发/生产数据
DB_PATH=./data/mengyu_eval.db
