# ===== Web 框架 =====
# 上限锁在主版本内，避免新环境 pip install 拉到破坏性新版（如 openai 2.x）
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
jinja2>=3.1,<4.0
itsdangerous>=2.1,<3.0       # 签名 cookie（会话）
python-multipart>=0.0.9,<1.0 # 表单 POST（注册/登录）
python-dotenv>=1.0,<2.0      # 读取 .env

# ===== LLM（OpenAI 兼容协议：DeepSeek / 智谱 / 通义 / Moonshot / new-api 等通用）=====
openai>=1.0,<2.0
# 注：SQLite 与密码哈希（pbkdf2）均使用 Python 标准库，无需额外依赖。

# ===== 星历（星座运势天文位置）=====
ephem>=4.2,<5.0

# ===== 黄历 / 干支（幸运色·方位·数字·宜忌 由当日干支推定）=====
lunar-python>=1.4.0,<2.0

# ===== 开发/测试 =====
pytest>=7.0,<9.0

# ===== 评估体系（evals harness / CI）=====
pyyaml>=6.0,<7.0   # thresholds.yaml / rubrics YAML 解析
